const BASE_PATHS = {
  top: '/',
  presidentMessage: '/president-message',
};

export const PAGES = {
  top: {
    root: BASE_PATHS.top,
  },
  presidentMessage: {
    root: BASE_PATHS.presidentMessage,
  },
};
